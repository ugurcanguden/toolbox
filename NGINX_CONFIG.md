# 🌐 Nginx Configuration for free-dev-tools.net.tr

Bu dosya, Nginx reverse proxy yapılandırması için gerekli ayarları içerir.

## 📋 Gereksinimler

- Nginx yüklü olmalı
- Domain DNS ayarları yapılmış olmalı (`free-dev-tools.net.tr` → `164.92.135.142`)
- SSL sertifikası (Let's Encrypt ile ücretsiz)

---

## 🔧 1. Nginx Kurulumu (Eğer yoksa)

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx -y

# Nginx'i başlat
sudo systemctl start nginx
sudo systemctl enable nginx

# Status kontrol
sudo systemctl status nginx
```

---

## 🌍 2. DNS Ayarları

Domain sağlayıcınızda (GoDaddy, Namecheap, vb.) A kaydı oluşturun:

```
Type: A
Host: toolbox
Value: 164.92.135.142
TTL: 3600 (veya default)
```

**Sonuç:** `free-dev-tools.net.tr` → `164.92.135.142`

DNS propagation kontrolü:
```bash
nslookup free-dev-tools.net.tr
# veya
dig free-dev-tools.net.tr
```

---

## 📝 3. Nginx Configuration

### A. Configuration Dosyası Oluştur

```bash
sudo nano /etc/nginx/sites-available/toolbox
```

### B. HTTP Configuration (İlk Kurulum)

```nginx
# Toolbox - HTTP Configuration (Temporary)
server {
    listen 80;
    listen [::]:80;
    
    server_name free-dev-tools.net.tr www.free-dev-tools.net.tr;
    
    # Logs
    access_log /var/log/nginx/toolbox-access.log;
    error_log /var/log/nginx/toolbox-error.log;
    
    # Reverse Proxy to Docker Container
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # Headers
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Cache bypass
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss application/rss+xml font/truetype font/opentype application/vnd.ms-fontobject image/svg+xml;
}
```

### C. Enable Site

```bash
# Symlink oluştur
sudo ln -s /etc/nginx/sites-available/toolbox /etc/nginx/sites-enabled/

# Default site'ı devre dışı bırak (opsiyonel)
sudo rm /etc/nginx/sites-enabled/default

# Nginx config test
sudo nginx -t

# Nginx'i reload et
sudo systemctl reload nginx
```

---

## 🔒 4. SSL Certificate (Let's Encrypt)

### A. Certbot Kurulumu

```bash
# Ubuntu/Debian
sudo apt install certbot python3-certbot-nginx -y
```

### B. SSL Sertifikası Al

```bash
# Otomatik Nginx yapılandırması ile
sudo certbot --nginx -d free-dev-tools.net.tr -d www.free-dev-tools.net.tr

# Email adresini gir
# Terms'i kabul et (Y)
# Email paylaşımı (N veya Y)
```

Certbot otomatik olarak:
- SSL sertifikası oluşturur
- Nginx config'i günceller
- HTTP'den HTTPS'e redirect ekler
- Otomatik yenileme için cron job ekler

### C. SSL Configuration (Certbot Sonrası)

Certbot, config'inizi şu şekilde güncelleyecek:

```nginx
# Toolbox - HTTPS Configuration (Auto-generated by Certbot)
server {
    listen 80;
    listen [::]:80;
    server_name free-dev-tools.net.tr www.free-dev-tools.net.tr;
    
    # Redirect to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    
    server_name free-dev-tools.net.tr www.free-dev-tools.net.tr;
    
    # SSL Certificate (managed by Certbot)
    ssl_certificate /etc/letsencrypt/live/free-dev-tools.net.tr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/free-dev-tools.net.tr/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    
    # Logs
    access_log /var/log/nginx/toolbox-access.log;
    error_log /var/log/nginx/toolbox-error.log;
    
    # Reverse Proxy to Docker Container
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # Headers
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_cache_bypass $http_upgrade;
    }
    
    # Security Headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss application/rss+xml font/truetype font/opentype application/vnd.ms-fontobject image/svg+xml;
}
```

### D. Otomatik SSL Yenileme Test

```bash
# Dry-run test
sudo certbot renew --dry-run

# Cron job kontrol (otomatik eklenir)
sudo systemctl status certbot.timer
```

---

## 🧪 5. Test ve Doğrulama

### A. HTTP → HTTPS Redirect Test

```bash
curl -I http://free-dev-tools.net.tr
# Beklenen: 301 Redirect to HTTPS
```

### B. HTTPS Test

```bash
curl -I https://free-dev-tools.net.tr
# Beklenen: 200 OK
```

### C. SSL Grade Check

Online araçlar:
- https://www.ssllabs.com/ssltest/analyze.html?d=free-dev-tools.net.tr
- Beklenen grade: A veya A+

### D. Browser Test

```
https://free-dev-tools.net.tr
```

Tarayıcıda yeşil kilit simgesi görünmeli ✅

---

## 🔥 6. Firewall Ayarları

```bash
# UFW ile (Ubuntu/Debian)
sudo ufw allow 'Nginx Full'
sudo ufw allow 'OpenSSH'
sudo ufw enable

# Status kontrol
sudo ufw status

# Beklenen output:
# To                         Action      From
# --                         ------      ----
# Nginx Full                 ALLOW       Anywhere
# OpenSSH                    ALLOW       Anywhere
```

---

## 📊 7. Monitoring & Logs

### Nginx Logs

```bash
# Access logs
sudo tail -f /var/log/nginx/toolbox-access.log

# Error logs
sudo tail -f /var/log/nginx/toolbox-error.log

# Son 100 satır
sudo tail -n 100 /var/log/nginx/toolbox-access.log
```

### Nginx Status

```bash
# Service status
sudo systemctl status nginx

# Test configuration
sudo nginx -t

# Reload (config değişikliğinden sonra)
sudo systemctl reload nginx

# Restart (gerekirse)
sudo systemctl restart nginx
```

---

## 🚨 8. Troubleshooting

### Problem: 502 Bad Gateway

```bash
# Docker container çalışıyor mu?
docker ps | grep toolbox

# Container logları
docker logs toolbox-prod

# Nginx error logs
sudo tail -f /var/log/nginx/toolbox-error.log

# Port kontrolü
sudo netstat -tlnp | grep :3000
```

### Problem: SSL Certificate Error

```bash
# Certificate durumu
sudo certbot certificates

# Manuel yenileme
sudo certbot renew

# Nginx config test
sudo nginx -t
```

### Problem: DNS Propagation

```bash
# DNS kontrol
nslookup free-dev-tools.net.tr
dig free-dev-tools.net.tr

# Farklı DNS server ile test
nslookup free-dev-tools.net.tr 8.8.8.8
```

---

## 🎯 9. Deployment Checklist

- [ ] DNS A kaydı oluşturuldu (`toolbox` → `164.92.135.142`)
- [ ] DNS propagation tamamlandı (nslookup ile kontrol)
- [ ] Nginx yüklendi ve çalışıyor
- [ ] Nginx config dosyası oluşturuldu (`/etc/nginx/sites-available/toolbox`)
- [ ] Site enable edildi (symlink oluşturuldu)
- [ ] Nginx config test passed (`nginx -t`)
- [ ] Docker container çalışıyor (port 3000)
- [ ] HTTP erişimi test edildi
- [ ] SSL sertifikası alındı (Certbot)
- [ ] HTTPS erişimi test edildi
- [ ] HTTP → HTTPS redirect çalışıyor
- [ ] Firewall ayarları yapıldı (port 80, 443)
- [ ] SSL grade A veya A+ (ssllabs.com)
- [ ] Browser'da yeşil kilit görünüyor ✅

---

## 📝 10. Quick Commands Reference

```bash
# Nginx
sudo systemctl start nginx        # Start
sudo systemctl stop nginx         # Stop
sudo systemctl restart nginx      # Restart
sudo systemctl reload nginx       # Reload config
sudo systemctl status nginx       # Status
sudo nginx -t                     # Test config

# Certbot
sudo certbot certificates         # List certificates
sudo certbot renew               # Renew certificates
sudo certbot delete              # Delete certificate

# Logs
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
journalctl -u nginx -f           # Systemd logs

# Docker
docker ps                        # Running containers
docker logs toolbox-prod         # Container logs
docker restart toolbox-prod      # Restart container
```

---

**Domain:** `free-dev-tools.net.tr`  
**Server IP:** `164.92.135.142`  
**Container Port:** `3000`  
**Public Ports:** `80` (HTTP), `443` (HTTPS)

**Son Güncelleme:** 2025-10-08
